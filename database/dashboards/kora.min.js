// Inisialisasi Firebase Authentication
var auth = firebase.auth();
var db = firebase.firestore();
var firestore = firebase.firestore();

db.collection("users").get().then(function (querySnapshot) {
    var total = querySnapshot.size;
    document.getElementById("users").textContent = total;
});

// Ux Design
db.collection("/koraa/items/mobile").get().then(function (querySnapshot) {
    var total = querySnapshot.size;
    document.getElementById("application").textContent = total;
});

db.collection("/koraa/items/design/ux design/web design").get().then(function (querySnapshot) {
    var total = querySnapshot.size;
    document.getElementById("web-design").textContent = total;
});

// Color
db.collection("/koraa/items/color").get().then(function (querySnapshot) {
    var total = querySnapshot.size;
    document.getElementById("colors").textContent = total;
});